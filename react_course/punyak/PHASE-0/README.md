Phase 0: Web & JavaScript Fundamentals

* 0.1 HTML & CSS Basics
> Semantic HTML structure (headings, forms, lists, divs vs semantic tags), CSS box model, flexbox, basic responsive layoutBuilding a couple of static pages by hand (no frameworks)

* 0.2 JavaScript Fundamentals
> Variables ( let / const ), data types, operatorsFunctions, scope,  this 
Arrays and objects, basic loopingDOM manipulation basics ( document.querySelector , event listeners) — helps them appreciate
what React automates later

* 0.3 Modern JavaScript (ES6+)
> Arrow functionsDestructuring, spread/rest operatorsTemplate literalsArray methods:  map ,  filter ,  reduce ,  find 
Modules ( import / export )

* 0.4 Asynchronous JavaScript
> Callbacks → Promises →  async / await 
 fetch  API — making a basic GET request to a public API (no auth needed yet)Understanding what an API response looks like (JSON)

* 0.5 Version Control Basics
> Git fundamentals: clone, commit, push, pull, branchesBasic GitHub workflow (since they'll use this constantly in React projects).

* Suggested checkpoint project: a small static page (e.g. a "profile card" or simple calculator) built with vanilla JS, no React — confirms fundamentals before moving on.